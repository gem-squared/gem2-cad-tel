# Streamlit Cloud reads this directly. Kept aligned with pyproject.toml,
# except opencv-python-headless is used here (no X11 libs needed in cloud).
# Local dev (pyproject.toml) can use either headless or full.

opencv-python-headless>=4.10.0
numpy>=1.26,<2.2
pdf2image>=1.17.0
pillow>=10.4.0
pydantic>=2.8.0
streamlit>=1.38.0
fastapi>=0.115.0
uvicorn>=0.30.0
paddleocr>=2.8.0
paddlepaddle>=2.6.1
